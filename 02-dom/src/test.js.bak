// MAIN.JS

// import domfuns from "./domfuns.js"

// console.log(idButton);



// document.body.addEventListener("click", e => {
//     console.log(e);
// });



// let counter = 0;


// idButton.addEventListener("click", e => {
//     console.log("You clicked here!");

//     const div = document.createElement("div");
//     div.appendChild(document.createTextNode("div"));
//     div.className = "clCard";
//     div.appendChild(document.createTextNode("This card has been created"));

//     idStuff2.insertAdjacentElement("afterend", div);

//     if (e.target.nodeName === "BUTTON") {
//         // Counter increases number count incrementally with each newly added div.
//         document.body.appendChild(domfuncs.buildCard("You just added " + counter++));
//     }
// });

// // domFunctions.js

// console.log(idStuff2)

// const functions = {

//     addBefore(node) {
//         // Not a pure function since there is NO RETURN, instead it mutates the parameters.
//         // Mock DOM
//         console.log("This function is being utilized.")
//         return null;
//     },

//     addAfter(node) {

//     },

//     delete() {

//     },

//     buildCard() {
//         console.log("In the buildCard function!");

//         const div = document.createElement("div");

//         div.appendChild(document.createTextNode("This has been inserted beside the adjacent div."));

//         const add = document.createElement("button");
//         // Adds text within the newly created button element.
//         add.appendChildElement(document.createTextNode("Add"));
//         // Adds the newly created button into the newly created div.
//         div.appendChild(Add);


//         const before = document.createElement("button");
//         before.appendChildElement(document.createTextNode("Before"))
//         div.appendChild(Before);

//         return card;
//     }

//     addBefore(node) {
//         console.log(node);
//         console.log(node.parent);

//     }

// }

// // Testing for the above functions.

// test("Does the dom functions work?", () => {
//     console.log("These tests are being utilized!");
//     const element = domfuncs.buildCard();
//     expect(element).toBeTruthy();

// });

// test("Does the add/before functions work?", () => {
//     const group = document.createElement("div");
//     console.log("We are in the add/before tests!");
//     const element = domfuncs.buildCard("First Insert");
//     group.appendChild(element);
//     expect(element).toBeTruthy;
//     expect(group.children.length).toBe(1);

//     // Pass in the node/element that is wanted to be inserted before.
//     domfuncs.appendChild(element);
//     expect(group.children.length).toBe(2);

//     const txt = "New Element!";
//     domfuncs.addBefore("element", txt);
//     expect(group.children.length).toBe(2);

//     expect(group.children[0].textContent.substr[0, 1]).toBe(txt);
//     expect(group.children[0].textContent.substr[0, 12]).toBe("");

//     expect(group.children[0].textContent).toBe(txt);
//     expect(group.children[1].textContent).toBe(txt);

//     expect(group.children[0].textContent).toBe("");

//     expect(group.children[1].textContent.)


// });