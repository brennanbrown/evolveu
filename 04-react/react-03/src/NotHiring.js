import React, { Component } from "react"

export const NotHiring = () =>
    <div>
        <p><em>(The library is not hiring. Check back later for more information.)</em></p>
    </div>

export default NotHiring